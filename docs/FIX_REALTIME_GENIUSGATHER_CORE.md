# Fix: RealtimeGeniusGatherCore

Added compatibility component:

- `components/RealtimeGeniusGatherCore.jsx`
- `components/RealtimeGeniusGatherCore.js`

Both wrap the existing `RealtimeConversationCore` so imports like `../../components/RealtimeGeniusGatherCore` resolve correctly.
