FINAL GatherGenius One-Package FIXED

Fix completed:
- BUILD WITHOUT TYPING now generates the prompt locally.
- Tap path no longer depends on /api/accessibility/interpret to work.
- If API/Supabase fails, UI still creates an Experience Lock in safe fallback mode.
- One-sentence input now supports shorthand budgets like $20k and 20k.
- Supabase insert warnings no longer break the build response.
- No nested ZIPs included.
- Ready for Vercel redeploy.
